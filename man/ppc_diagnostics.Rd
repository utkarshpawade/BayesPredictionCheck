% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppc_diagnostics.R
\name{ppc_diagnostics}
\alias{ppc_diagnostics}
\title{Posterior Predictive Diagnostic Statistics}
\usage{
ppc_diagnostics(y_obs, y_rep, credible_mass = 0.95)
}
\arguments{
\item{y_obs}{Numeric vector of length \eqn{n} containing the observed
outcomes.}

\item{y_rep}{Numeric matrix of dimension \eqn{S \times n} where each row is
one posterior predictive replicate (e.g. the output of \code{\link[=simulate_ppc]{simulate_ppc()}}).}

\item{credible_mass}{Numeric scalar in \eqn{(0, 1)} specifying the
credible interval mass used for the coverage calculation.
Defaults to \code{0.95}.}
}
\value{
An object of class \code{"ppc_diagnostics"} (a named list) with the
following elements:
\describe{
\item{\code{mean_diff}}{Difference between the mean of the replicated means
and the observed mean: \eqn{\bar{\mu}_{rep} - \bar{y}}.}
\item{\code{var_diff}}{Difference between the mean of the replicated variances
and the observed variance: \eqn{\bar{v}_{rep} - \mathrm{Var}(y)}.}
\item{\code{bayesian_p_value}}{Proportion of draws for which the replicated
mean exceeds the observed mean.  Values near 0.5 indicate good
calibration.}
\item{\code{rmse}}{Root mean squared error between the column-wise predictive
means and the observed values:
\eqn{\sqrt{n^{-1}\sum_i (\bar{y}^{rep}_i - y_i)^2}}.}
\item{\code{coverage}}{Proportion of observations whose value falls inside the
\code{credible_mass} posterior predictive interval.}
\item{\code{n_draws}}{Number of posterior draws \eqn{S}.}
\item{\code{n_obs}}{Number of observations \eqn{n}.}
\item{\code{credible_mass}}{The credible mass used for the coverage
calculation.}
}
}
\description{
Computes a suite of predictive discrepancy statistics that quantify how well
the posterior predictive distribution \eqn{p(y^{rep} \mid y)} matches the
observed data \eqn{y}.  All statistics are scalar summaries derived from
the \eqn{S \times n} replicated data matrix.
}
\examples{
set.seed(1)
y     <- rnorm(50, mean = 2, sd = 1)
draws <- matrix(rnorm(200 * 50, mean = 2), nrow = 200, ncol = 50)
y_rep <- simulate_ppc(draws)
diag  <- ppc_diagnostics(y, y_rep)
print(diag)

}
\seealso{
\code{\link[=simulate_ppc]{simulate_ppc()}}, \code{\link[=print.ppc_diagnostics]{print.ppc_diagnostics()}}
}
