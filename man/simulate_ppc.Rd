% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_ppc.R
\name{simulate_ppc}
\alias{simulate_ppc}
\title{Generate Posterior Predictive Samples}
\usage{
simulate_ppc(
  posterior_draws,
  X = NULL,
  family = "gaussian",
  sigma_posterior = NULL,
  n_trials = 1L
)
}
\arguments{
\item{posterior_draws}{A numeric matrix of dimension
\eqn{S \times P}, where \eqn{S} is the number of posterior iterations and
\eqn{P} the number of parameters.  When \code{X} is \code{NULL}, \eqn{P} is
interpreted as the number of observations \eqn{n} and each column is the
marginal posterior mean for one observation.  When \code{X} is supplied,
\eqn{P} must equal \code{ncol(X)}.}

\item{X}{Optional numeric design matrix of dimension \eqn{n \times P}.
If provided, the linear predictor \eqn{X \beta} is computed for each
posterior draw.  Defaults to \code{NULL}.}

\item{family}{Character string specifying the likelihood family.
Currently supported: \code{"gaussian"} and \code{"binomial"}.
Defaults to \code{"gaussian"}.}

\item{sigma_posterior}{Optional numeric vector of length \eqn{S} containing
posterior draws of the residual standard deviation \eqn{\sigma}.  Only
used when \code{family = "gaussian"}.  If \code{NULL}, the empirical standard
deviation of the linear predictor across observations is used as a
fallback.}

\item{n_trials}{Integer. Number of binomial trials per observation.  Only
used when \code{family = "binomial"}.  Defaults to \code{1} (Bernoulli).}
}
\value{
A numeric matrix of dimension \eqn{S \times n} containing one
replicated data set per posterior draw.
}
\description{
Generates replicated outcome samples \eqn{y^{rep}} from a matrix of
posterior draws under a specified likelihood family.  Each row of
\code{posterior_draws} is treated as one posterior sample of the parameter
vector \eqn{(\mu_1, \ldots, \mu_n)} (Gaussian) or \eqn{(p_1, \ldots, p_n)}
(Binomial), where \eqn{n} is the number of observations.  When a design
matrix \code{X} is supplied the linear predictor
\eqn{\eta = X \beta} is computed first and the appropriate inverse-link is
applied.

The posterior predictive distribution is
\deqn{p(y^{rep} \mid y) = \int p(y^{rep} \mid \theta)\, p(\theta \mid y)\, d\theta.}
This function approximates the integral by averaging over the discrete
posterior samples.
}
\details{
For \code{family = "gaussian"}, each replicated observation is drawn as
\deqn{y^{rep}_{si} \sim \mathcal{N}(\mu_{si},\, \sigma_s^2),}
where \eqn{\mu_{si}} is the fitted mean for draw \eqn{s} and observation
\eqn{i}.

For \code{family = "binomial"}, the linear predictor is passed through the
logistic function \eqn{p = 1 / (1 + e^{-\eta})} and
\deqn{y^{rep}_{si} \sim \mathrm{Binomial}(n\_trials,\, p_{si}).}
}
\examples{
set.seed(42)
S <- 200   # posterior draws
n <- 50    # observations
draws <- matrix(rnorm(S * n, mean = 2), nrow = S, ncol = n)
y_rep  <- simulate_ppc(draws, family = "gaussian")
dim(y_rep)  # 200 x 50

}
\seealso{
\code{\link[=ppc_diagnostics]{ppc_diagnostics()}}, \code{\link[=plot_ppc_overlay]{plot_ppc_overlay()}}
}
