% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppc_visualization.R
\name{plot_ppc_overlay}
\alias{plot_ppc_overlay}
\title{Posterior Predictive Density Overlay Plot}
\usage{
plot_ppc_overlay(y_obs, y_rep, n_samples = 50, ...)
}
\arguments{
\item{y_obs}{Numeric vector of length \eqn{n} containing the observed
outcomes.}

\item{y_rep}{Numeric matrix of dimension \eqn{S \times n} where each row is
one posterior predictive replicate (e.g. the output of \code{\link[=simulate_ppc]{simulate_ppc()}}).}

\item{n_samples}{Positive integer.  Number of rows to subsample from
\code{y_rep} for the overlay.  Subsampling improves readability when \eqn{S}
is large.  Defaults to \code{50}.  The subsample is drawn without replacement
when \code{n_samples < S}, otherwise all rows are used.}

\item{...}{Additional arguments passed to \code{\link[bayesplot:PPC-distributions]{bayesplot::ppc_dens_overlay()}}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Wraps \code{\link[bayesplot:PPC-distributions]{bayesplot::ppc_dens_overlay()}} to produce a density overlay plot
comparing the observed data distribution against a random subsample of
posterior predictive replicates.  The plot is returned as a \code{ggplot2}
object and styled with \code{\link[=theme_ppc]{theme_ppc()}}.

A density overlay is one of the most diagnostic plots in the posterior
predictive checking toolkit: if the observed density (dark line) falls
within the envelope formed by the replicated draws (light lines), the model
provides an adequate description of the marginal distribution of \eqn{y}.
}
\examples{
set.seed(7)
y     <- rnorm(80, mean = 0, sd = 1)
draws <- matrix(rnorm(300 * 80, mean = 0), nrow = 300, ncol = 80)
y_rep <- simulate_ppc(draws)
p     <- plot_ppc_overlay(y, y_rep, n_samples = 40)
\dontrun{
  print(p)
}

}
\seealso{
\code{\link[=plot_ppc_stat]{plot_ppc_stat()}}, \code{\link[bayesplot:PPC-distributions]{bayesplot::ppc_dens_overlay()}}
}
